### 7.2.1 Spring AI 简介与配置

集成大模型到 Java 后端，一个常见的工程问题是：每个模型提供商都有自己的 SDK、协议和认证方式。如果 AIoT 平台要同时接入 GPT、Claude、DeepSeek 和通义千问，就得维护四套独立的 HTTP 客户端、错误处理和重试逻辑。Spring AI 恰好把这个胶水层抽了出来——它为 Java/Spring 生态提供了一套统一的 AI 模型调用抽象，切换模型提供商的成本被压缩到仅修改几行配置。

从设计定位看，Spring AI 更像一个适配器层，而不是独立的“Agent 框架”。核心接口 `ChatClient` 屏蔽了底层模型差异——它接收的是 OpenAI Chat Completions 风格的调用，输出的是标准化的 `ChatResponse`。对业务代码来说，无论后端连的是 OpenAI、Anthropic 还是 Ollama 本地模型，调用方式完全一致。除文本对话外，Spring AI 还提供了 `ImageClient`、`EmbeddingClient`、`AudioClient` 等接口。不过对物联网场景而言，`ChatClient` 与 Function Calling 的组合才是真正的核心能力——后者让模型能够按需调用 Java 方法获取实时数据或执行设备操作。Spring AI 将这种机制封装为 `@Tool` 注解和 `ToolCallback` 接口，开发者只需写一个普通方法，框架自动将其注册为可供模型调用的工具描述。

**为什么 IoT DC3 不接入一个独立的 AI 网关，而是选用 Spring AI？** 工程上有一个很直接的原因：DC3 是纯 Java 21 / Spring Boot 4 平台，每个服务都运行在 JVM 中。为引入 AI 再部署一套 Python 运行时，意味着要额外维护一个容器、一条语言桥接，以及两份运行时之间的网络通信可靠性。Spring AI 把 AI 能力拉近了现有 Java 进程：`ChatClient` 本身就是一个 Spring Bean，工具调用直接走 JVM 内的普通 Java 方法调用，认证上下文通过 Spring Security 过滤器链流转。模型拿到的设备数据和操作权限，不会超出操作员账号本身的 RBAC 范围。这种原生集成方式让工具方法可以直接引用业务 Service 层的代码，租户隔离在方法注解层面就能完成，不必额外维护一套权限等价映射。从运维视角看，这意味着少了一道需要部署、排障的中间环节，也少了一个跨语言调用的超时隐患。

配置入口很清晰。在现有 Spring Boot 项目中引入 Spring AI 只需要两步。第一步，在 `pom.xml` 中加入 Spring AI 的 BOM 和核心依赖：

```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-bom</artifactId>
            <version>1.0.0-M3</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
    </dependency>
</dependencies>
```

第二步，在 `application.yml` 中配置模型提供商。Spring AI 默认使用 OpenAI Chat Completions 协议——这意味着只要后端暴露一个 OpenAI 兼容的端点，就能接入任何模型。下面展示了从云端模型切换到本地 Ollama 的配置方式：

```yaml
# 云端 GPT
spring:
  ai:
    openai:
      base-url: https://api.openai.com
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o

---
# 本地 Ollama（假设运行在 localhost:11434）
spring:
  ai:
    openai:
      base-url: http://localhost:11434/v1
      api-key: not-needed
      chat:
        options:
          model: deepseek-r1:7b
```

这两份配置共用同一套 `ChatClient` 代码。切换时只需激活对应的 Spring Profile，不需要改任何 Java 代码。DC3 的 Agentic Center 更进一步：支持在业务层面按会话指定不同的模型提供商，运维人员可以按成本、隐私或时延要求动态切换，而不必在环境级别硬编码。

配置中还可以自定义模型参数：`temperature` 控制输出的随机性，`max-tokens` 限制单次响应的最大长度，`stream` 开关决定是否启用流式输出。在物联网运维场景里，流式输出对交互体验很关键——当模型调用 `DeviceTool` 查询多个点位时，如果采用流式，操作员可以一边看模型“正在查询锅炉温度和风机转速”，一边等待完整结果，而不是面对空白界面猜测进度。下面是一个带参数的配置示例：

```yaml
spring:
  ai:
    openai:
      chat:
        options:
          temperature: 0.1      # 运维场景用低温度保证确定性
          max-tokens: 4096
          stream: true
```

配置完成后，开发者只需在代码中注入 `ChatClient` 即可使用。例如，在 Spring 组件中：

```java
@SpringBootTest
public class ChatClientExample {
    @Autowired
    private ChatClient chatClient;

    public void ask() {
        String response = chatClient.call("查询锅炉当前温度");
        System.out.println(response);
    }
}
```

实际使用时往往需要带上系统提示词和工具定义，Spring AI 提供了 `ChatClient.builder()` 模式来组装这些元数据。这些内容将在后续小节展开。

```book-figure
id: fig-7-1
type: sequence
title: 图7-1 Spring AI 集成架构——从配置到响应的请求链路
purpose: 展示 Spring AI Client Starter 如何将 application.yml 中的配置参数转换为一次标准的 OpenAI 兼容请求，并将 AI 模型响应返回给业务代码。
audience_takeaway: 读者应理解Spring AI 集成架构——从配置到响应的请求链路中的主链路、责任边界和工程取舍。
visual_focus: 从Application Code到终点的主链路。
design_level: implementation
layout: 自上而下，5 条垂直生命线：Application Config、Spring AI Client Starter、OpenAI API Adapter、AI Model Provider、Application Code
elements:
- Application Config：配置文件，内含 base-url、api-key、model 三个参数
- Spring AI Client Starter：自动配置类，读取配置后生成 ChatClient Bean
- OpenAI API Adapter：协议适配层，将 ChatClient 的通用请求翻译为 OpenAI Chat Completions 格式的 JSON，并对返回结果解包
- AI Model Provider：后端模型服务（OpenAI、DeepSeek、Ollama 等），接收 HTTP POST 请求，返回流式或非流式响应
- Application Code：业务代码，注入 ChatClient 后调用 call/send 等方法
relationships:
- Application Code 请求 Spring AI Client Starter 创建 ChatClient
- Spring AI Client Starter 读取 Application Config，实例化 ChatClient
- Application Code 调用 ChatClient.call(prompt)
- ChatClient 将 prompt 交由 OpenAI API Adapter 封装为 HTTP POST 请求（JSON）
- OpenAI API Adapter 将请求发送到 AI Model Provider 的 /v1/chat/completions 端点
- AI Model Provider 返回 JSON 响应，Adapter 解析为 ChatResponse 对象
- Adapter 将 ChatResponse 返回给 Application Code
regions:
- id: platform_domain
  label: 平台服务域
  role: 核心服务能力边界
- id: intelligence_domain
  label: 智能决策域
  role: 模型、规则与 Agent 边界
components:
- id: r1
  label: Application Code
  type: platform
  subtitle: ''
  group: platform_domain
  priority: primary
  shape: card
- id: r2
  label: ChatClient.call
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r3
  label: OpenAI API Adapte…
  type: ai
  subtitle: ''
  group: intelligence_domain
  priority: normal
  shape: card
- id: r4
  label: AI Model Provider…
  type: ai
  subtitle: ''
  group: intelligence_domain
  priority: normal
  shape: card
connections:
- from: r1
  to: r2
  label: prompt
  style: solid
  direction: request
- from: openai_api_adapter
  to: ai_model_provider
  label: OpenAI API Adapte…
  style: solid
  direction: request
callouts:
- Application Code 请求 Spring AI Client Starter 创建 ChatClient
- Spring AI Client Starter 读取 Application Config，实例化 ChatCl…
- Application Code 调用 ChatClient.call(prompt)
legend:
- 六边形表示外部系统（AI Provider）
- 圆角矩形表示 Spring Boot Bean
- 实线箭头表示同步调用，虚线箭头表示异步返回
- 颜色：Bean 用蓝色，适配器用灰色，外部服务用绿色（本地模型）/深蓝（云端模型）
caption: 图7-1 Spring AI 集成架构。OpenAI API Adapter 是透明的代理层——它只做协议翻译，不引入额外逻辑。无论后端是云端 GPT 还是本地 Ollama，上层的 ChatClient 调用代码完全不变。
visual_constraints:
- 节点标签使用短名词短语，解释性文字放入 callouts 或正文。
- 图例放在底部，不遮挡主体结构。
render_notes: 生命线自上而下，时间轴垂直；参与者名称居中；箭头标签使用动词短语；响应流用虚线显示；本地与云端模型通过不同颜色区分。
```

总之，Spring AI 让 Java 工程师在 AIoT 集成中少踩许多跨语言、跨版本的坑，代价仅仅是多一个 Maven 依赖和几行配置。下一节将展示如何在物联网对话中真正利用这一能力——通过 Function Calling 让模型读取位号、控制设备。