### 7.5.2 Agent 模式：自主决策与执行

Copilot 模式把执行按钮留在人手里，Agent 模式则把这个按钮交给了模型——运维人员只需要给出目标，Agent 自主规划步骤、依次调用工具、监控执行结果，直到任务完成或遇到无法处理的障碍。这不是简单的功能升级，而是物联网运维范式的根本变化：从“人用模型查数据”变成了“模型替人去干活”。

**Agent 的核心工作循环**是“感知 — 规划 — 执行 — 反馈”的闭环。当操作员在 Agentic Center 中提交一个目标（比如“检查二号车间的网络连通性，如果发现设备离线则尝试重启”），Agent 首先调用 `DeviceTool` 查询该车间所有设备的在线状态，感知当前系统状况。接着，它将任务分解为子步骤——识别离线设备、检查离线持续时长、判断是否需要重启。规划完成后，Agent 逐一调用 `CommandTool` 执行设备重启指令，每执行一步都读取返回结果，确认设备是否恢复在线。如果某台设备重启后仍然离线，Agent 会标记异常，记录失败原因，并继续处理下一台设备，最后汇总一份执行报告给操作员。

这个循环依赖两个前提条件。第一个是**工具集合的完备性**——Agent 需要足够的“操作手柄”才能完成任务分解。IoT DC3 的 Agentic Center 内置 10 个 `@Tool` 工具，覆盖设备、驱动、点位、命令、事件等全部领域对象（资料：[S9]）。这意味着 Agent 面对“排查最近一小时温度超限的设备并生成干预建议”这类复合指令时，可以自主调用 `PointValueTool` 获取实时数据、调用 `DeviceTool` 关联设备元信息、调用 `EventTool` 查询历史告警，最后组合成一份结构化输出。若缺少任何一个工具，任务就会卡在某个环节。

第二个前提是**信任机制的建立**。Agent 自主执行的风险比 Copilot 高一个数量级——模型一个错误规划可能导致批量设备误动作。IoT DC3 的设计采用三层安全阀：

*   **操作日志审计**：每一次工具调用、每一次写入操作的入参和返回结果都会被完整记录，事后可以复现任意 Agent 会话的全链路。
*   **人类审批节点**：高风险操作（如批量写入设备参数、启停关键设备驱动）会被拦截，等待操作员在界面上确认后才能放行（资料：[S6]）。审批条件按照风险等级配置，例如对写位号值超过阈值的操作一律挂起，对只读查询则自动放行。
*   **回滚能力**：对于可逆的操作，Agent 需要先记录操作前状态（例如设备原参数快照），执行异常时能够发起反向操作恢复原值。对于不可逆的操作（如固件升级），Agent 不允许自动执行，必须由人工介入。

在这三层安全阀的保护下，Agent 的自主范围可以逐步扩大：先开放只读的查询工具，再开放低风险写操作（如修改非关键告警阈值），最后开放需要审批的高风险写操作。这种渐进式授权模式借鉴了工业控制的安全分级思想，确保任何误操作都不会超出预期边界。

以下是一个完整的假设场景，展示 Agent 从感知到执行再到汇报的完整链路：

> 凌晨 2:00，Agent 发现三号车间的若干台设备同时离线，持续超过 10 分钟。Agent 首先调用 `DriverTool` 检查驱动实例状态，确认驱动进程正常运行；接着调用 `DeviceTool` 对离线设备进行连通性测试，发现一部分设备网络不可达、另一部分设备可 ping 通但数据上报中断。Agent 自主执行“重启网络不可达设备的网口”操作（该操作被配置为低风险，自动放行），同时标记“数据上报中断但网络正常”的异常，生成一段自然语言描述：“三号车间三台设备离线（IP 不可达），已执行重启网口命令并等待恢复；两台设备网络正常但数据中断，可能与采集程序卡死有关，建议联系现场人员检查。详细日志见附件。”

```book-figure
id: fig7-7
type: flowchart
title: Agent 自主运维流程示意图
purpose: 展示 Agent 从接收目标到完成报告的完整工作循环，包括信任机制的三层保护
audience_takeaway: 读者应理解Agent 自主运维流程示意图中的主链路、责任边界和工程取舍。
visual_focus: 从start到report的主链路。
design_level: implementation
layout: 自上而下、从左到右分层，主要流程纵向排列，次要分支横向展开
elements:
- 操作员提交目标
- 'Agent 感知系统状态

  (调用 DeviceTool / PointValueTool)'
- 'Agent 分解任务

  (生成子步骤清单)'
- '调用工具执行子步骤

  (遍历子步骤，调用 CommandTool 等)'
- 检查执行结果
- 记录异常并上报
- 判断是否需要人工审批
- '等待操作员确认

  (带超时)'
- 汇总执行报告
- 反馈给操作员
relationships:
- start → sense（输入目标）
- sense → plan（感知数据）
- plan → execute（规划完成）
- execute → check（执行完毕）
- check → fail（失败）
- check → approval（成功）
- fail → report（汇聚错误）
- approval → wait_confirm（需要审批）
- approval → execute（无需审批/继续下一子步骤）
- wait_confirm → execute（确认通过）
- wait_confirm → report（超时或拒绝）
- approval → report（无下一子步骤）
regions:
- id: platform_domain
  label: 平台服务域
  role: 核心服务能力边界
components:
- id: r1
  label: start
  type: platform
  subtitle: ''
  group: platform_domain
  priority: primary
  shape: card
- id: r2
  label: sense
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r3
  label: plan
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r4
  label: execute
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r5
  label: check
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r6
  label: fail
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r7
  label: approval
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r8
  label: report
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r9
  label: wait_confirm
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
- id: r10
  label: end
  type: platform
  subtitle: ''
  group: platform_domain
  priority: normal
  shape: card
connections:
- from: r1
  to: r2
  label: 输入目标
  style: solid
  direction: request
- from: r2
  to: r3
  label: 感知数据
  style: solid
  direction: request
- from: r3
  to: r4
  label: 规划完成
  style: solid
  direction: request
- from: r4
  to: r5
  label: 执行完毕
  style: solid
  direction: request
- from: r5
  to: r6
  label: 失败
  style: dashed
  direction: request
- from: r5
  to: r7
  label: 成功
  style: solid
  direction: request
- from: r6
  to: r8
  label: 汇聚错误
  style: dashed
  direction: request
- from: r7
  to: r9
  label: 需要审批
  style: risk
  direction: request
- from: r7
  to: r4
  label: 无需审批/继续下一子步骤
  style: solid
  direction: request
- from: r9
  to: r4
  label: 确认通过
  style: solid
  direction: request
- from: r9
  to: r8
  label: 超时或拒绝
  style: dashed
  direction: request
- from: r7
  to: r8
  label: 无下一子步骤
  style: solid
  direction: request
callouts:
- start → sense（输入目标）
- sense → plan（感知数据）
- plan → execute（规划完成）
legend:
- 起始/结束节点
- 过程/动作节点
- 判断/分流节点
- 异常处理节点
- 等待人机交互节点
- 高风险流程（需要审批）
caption: 图7-7 Agent 自主运维流程示意图。图中虚线框表示信任机制层，涵盖操作日志审计、人类审批节点和回滚能力。红色箭头表示高风险操作需要等待人工确认，绿色箭头表示低风险操作自动执行。Agent 在完成报告后仍可通过持续监控循环重新感知系统状态。
visual_constraints:
- 节点标签使用短名词短语，解释性文字放入 callouts 或正文。
- 图例放在底部，不遮挡主体结构。
- 决策节点必须写成可判断的问题或动作，分支标签保持短句。
render_notes: 使用 SVG 绘制。节点位置相对坐标自适应。起始/结束节点用浅绿色椭圆，部分矩形节点用浅蓝色（主流程）、浅粉色（异常处理）、浅灰色（等待确认）。判断节点用黄色菱形。信任机制层用浅灰色虚线矩形包围，框内包含元素“check
  → fail → approval → wait_confirm → report”以及相应连线。高风险连线用红色实线，低风险连线用绿色实线，反馈路径用灰色虚线。为每个节点添加 tooltip 悬浮显示详细说明（例如元素 sense 的 tooltip
  内容：“调用 DeviceTool 查询设备列表，调用 PointValueTool 获取实时位号值”）。
```

Agent 模式并非要完全取代 Copilot 模式，而是提供了另一种工作方式的选择。对于经验丰富的运维工程师，Copilot 模式依然有价值——他只需要模型提供信息，自己做出决策。但对于夜间值班、快速响应、例行巡检等场景，Agent 模式能显著提升效率。实际部署时，运维团队可以根据任务风险等级和自身信任程度，为不同任务配置不同的模式偏好，甚至在同一会话中混合使用：先通过 Copilot 模式验证方案，再切换到 Agent 模式让模型自主执行已批准的步骤。