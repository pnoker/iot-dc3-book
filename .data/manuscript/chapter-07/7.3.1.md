### 7.3.1 IoT DC3 Agentic Center 架构概述

操作员发现产线温度异常，需要在一个界面查位号值，切到设备管理找风机执行器，再回到命令界面下发转速调整。数据在、接口在，但操作路径割裂是物联网运维常见的工程瓶颈。Agentic Center 要打破的正是这层界面锁死——让大语言模型在平台上能读能写，把一个跨界面的操作链压缩成一句自然语言。

#### IoT DC3 平台简况

IoT DC3 是一个开源物联网基础平台，核心服务以微服务形式运行。在进入 Agentic Center 之前，DC3 已经具备四层参考架构的完整能力：设备接入、设备管理、数据汇聚、基础运维工具。这些能力封装在几大中心服务中——`dc3-center-manager` 处理设备元数据，`dc3-center-data` 负责位号值的读写与存储，`dc3-center-auth` 管理租户身份和 RBAC 权限校验（资料：[S7]）。DC3 对数据的抽象是“位号”——一个附带了语义的物理量描述，例如`锅炉_温度`。这种抽象使得平台向上提供的是标准化位号值接口，而非原始传感器报文或寄存器地址。

#### Agentic Center 的设计目标

Agentic Center 不是新建独立的 AI 子系统，而是把大语言模型嵌入 DC3 现有的运维流程，让 AI 既能读取平台数据，也能操作平台设备（资料：[S6]）。设计上遵循两条硬性原则：

1. **所有 AI 操作最终走平台真实 API**。模型不直接访问数据库，不绕过鉴权中心。每次工具调用都经过网关注入用户主体上下文，走 RBAC 权限校验——AI 拿到的权限不超过操作员账号自身的权限。这条原则保障了 AI 不会成为权限后门。
2. **工具就是平台服务层的映射**。Agentic Center 内置的 `@Tool` 工具类，每个都是对已有服务层方法的薄包装，不重复业务逻辑（资料：[S6]）。已有的设备管理、数据读写、驱动启停功能，只需在方法上标注 `@Tool` 注解，就能暴露给大模型。工具集的维护成本几乎为零——平台每增加一个 API，对应的工具可以同步跟上。

架构选择 Spring AI 的原因有两个。一是原生 JVM 集成：DC3 是纯 Java 21 平台，引入 Spring AI 无需额外 Python 运行时、不必管理两个语言运行时之间的网络桥接。`ChatClient` 是一个 Spring Bean，工具调用是普通 Java 方法调用，认证上下文走已有的 Spring Security 过滤器链。二是协议兼容性：Spring AI 的 `ChatClient` 默认采用 OpenAI Chat Completions 协议，可以接入任何提供 OpenAI 兼容端点的模型——GPT、Claude、DeepSeek、通义千问，甚至本地运行的 Ollama 实例（资料：[S6]）。

#### 架构层次：从设备到对话的四层结构

从部署和职责视角，Agentic Center 的架构可以拆成四层：

- **用户交互层**：操作人员看到的入口，可以是 DC3 Web 界面中嵌入的聊天窗口，也可以通过 `POST /api/v3/agentic/v1/chat/completions` 接入的任意客户端。消息格式兼容 OpenAI Chat Completions 规范，支持流式响应和非流式请求。
- **AI Agent 层**：核心是 `dc3-center-agentic` 服务，包含一个 Spring AI 的 `ChatClient` Bean，加载了多个 `@Tool` 工具方法。会话管理（`MessageChatMemoryAdvisor`）和模型路由配置也位于这一层。AI Agent 层只做意图识别与工具调度，业务请求通过 Feign 客户端转发给下一层。
- **平台服务层**：包括管理、数据、鉴权等中心服务。Agentic Center 通过 Feign 客户端调用这些服务，方式与普通前端请求完全一致——不走旁路，不设特殊通道。
- **数据与设备层**：底层包括 PostgreSQL（关系数据）、MongoDB（位号历史值时序存储）、以及通过驱动网关连接的物理设备。设备本身对 AI 不可见，AI 看到的是经过归一化的位号值。

```book-figure
id: "fig-7-6"
type: "layered"
title: "图7-6 Agentic Center 四层架构示意图"
purpose: "展示Agentic Center如何将AI Agent层叠加在DC3现有三层平台服务之上，形成四层结构，说明每一层的职责、组件和层间通信方式"
audience_takeaway: "读者应理解AI Agent层是调度层，不做推理；所有数据操作必须通过平台服务层，不走旁路。"
visual_focus: "AI Agent层与平台服务层之间的Feign调用链路为主线，标注“权限校验不绕过”。"
design_level: "logical"
layout: "从上至下四层堆叠：用户交互层、AI Agent层、平台服务层、数据设备层"
elements:
  - "用户交互层：Web UI对话窗口、外部客户端（CLI/第三方应用）"
  - "AI Agent层：dc3-center-agentic服务、ChatClient Bean、@Tool 工具、会话管理（MessageChatMemoryAdvisor）、模型路由"
  - "平台服务层：dc3-center-manager（设备元数据）、dc3-center-data（位号值读写）、dc3-center-auth（RBAC权限）"
  - "数据设备层：PostgreSQL、MongoDB、物理设备（PLC/传感器/执行器）"
relationships:
  - "用户交互层→AI Agent层：POST /api/v3/agentic/v1/chat/completions（OpenAI协议）"
  - "AI Agent层→平台服务层：Feign客户端调用（RESTful接口）"
  - "平台服务层→数据设备层：JDBC/设备驱动网关"
  - "所有层间调用必须经过鉴权：AI Agent层不绕过dc3-center-auth"
regions:
  - id: "user_region"
    label: "用户交互域"
    role: "对接各类客户端"
  - id: "agent_region"
    label: "AI Agent域"
    role: "意图识别与工具调度"
  - id: "platform_region"
    label: "平台服务域"
    role: "数据与鉴权"
  - id: "data_region"
    label: "数据设备域"
    role: "存储与物理设备"
components:
  - id: "web_ui"
    label: "Web 对话窗口"
    type: "application"
    subtitle: "平台内嵌界面"
    group: "user_region"
    priority: "normal"
    shape: "card"
  - id: "external_client"
    label: "外部客户端"
    type: "application"
    subtitle: "CLI / 第三方"
    group: "user_region"
    priority: "normal"
    shape: "card"
  - id: "agentic_center"
    label: "agentic 中心"
    type: "ai"
    subtitle: "ChatClient + @Tool"
    group: "agent_region"
    priority: "primary"
    shape: "card"
  - id: "manager_service"
    label: "设备管理服务"
    type: "platform"
    subtitle: "设备/驱动/模板"
    group: "platform_region"
    priority: "primary"
    shape: "card"
  - id: "data_service"
    label: "数据读写服务"
    type: "platform"
    subtitle: "位号值实时/历史"
    group: "platform_region"
    priority: "primary"
    shape: "card"
  - id: "auth_service"
    label: "鉴权服务"
    type: "platform"
    subtitle: "RBAC 租户隔离"
    group: "platform_region"
    priority: "primary"
    shape: "card"
  - id: "postgresql"
    label: "PostgreSQL"
    type: "data"
    subtitle: "设备/用户/会话"
    group: "data_region"
    priority: "normal"
    shape: "database"
  - id: "mongodb"
    label: "MongoDB"
    type: "data"
    subtitle: "位号时序历史"
    group: "data_region"
    priority: "normal"
    shape: "database"
  - id: "physical_devices"
    label: "物理设备"
    type: "edge"
    subtitle: "PLC/传感器/执行器"
    group: "data_region"
    priority: "supporting"
    shape: "card"
connections:
  - from: "web_ui"
    to: "agentic_center"
    label: "HTTP/SSE 流式"
    style: "solid"
    direction: "request"
  - from: "external_client"
    to: "agentic_center"
    label: "REST API"
    style: "dashed"
    direction: "request"
  - from: "agentic_center"
    to: "manager_service"
    label: "Feign 调用"
    style: "solid"
    direction: "request"
  - from: "agentic_center"
    to: "data_service"
    label: "Feign 调用"
    style: "solid"
    direction: "request"
  - from: "agentic_center"
    to: "auth_service"
    label: "Feign 鉴权"
    style: "dashed"
    direction: "async"
  - from: "manager_service"
    to: "postgresql"
    label: "JDBC"
    style: "solid"
    direction: "request"
  - from: "data_service"
    to: "mongodb"
    label: "JDBC"
    style: "solid"
    direction: "request"
  - from: "data_service"
    to: "physical_devices"
    label: "驱动网关"
    style: "dashed"
    direction: "async"
callouts:
  - "AI Agent层本身不进行模型推理，推理在远端/本地模型服务中完成。"
  - "所有工具调用必须通过平台服务层的鉴权服务，AI不能绕过RBAC。"
legend:
  - "蓝色：平台服务层组件；橙色：AI Agent层；青绿色：设备与数据存储；灰色：外部/用户界面。"
  - "实线箭头：同步HTTP请求；虚线箭头：异步事件或可选调用。"
caption: "图7-6 Agentic Center 四层架构示意图。从上至下依次为用户交互层、AI Agent层、平台服务层、数据设备层，展示了各层组件及层间通信方式。"
visual_constraints:
  - "最多10个主节点，每层2-3个组件。"
  - "AI Agent层用橙色强调，平台服务层用蓝色，数据设备层用青绿色。"
  - "连接线只标注主要调用路径，避免全图连线过多。"
render_notes: "HTML/SVG渲染，浅色背景，圆角矩形卡片，层间实线箭头标注调用方式，图例置于底部。"
```

#### 工具即是能力：内置工具集

Agentic Center 出厂自带的多个 `@Tool` 工具类，覆盖平台最主要的领域对象。其中 `DeviceTool` 用于查询设备状态与详情，`DriverTool` 管理驱动启停，`PointValueTool` 读取和写入位号值——这三个工具直接支撑了“对话驱动设备操作”的核心链路（资料：[S6]）。每个工具方法上的 `@Tool` 注解包含 `description` 属性，描述工具的能力和参数，这些描述被注入模型上下文，帮助模型判断何时调用哪个工具。

#### 架构之外的权衡

Agentic Center 有一个重要的“不为”选择：不做模型推理。Agentic Center 只负责意图识别和工具调度，真正的模型推理发生在远端或本地部署的模型服务中。这意味着 Agentic Center 不部署大模型，不管理 GPU 资源。模型推理和平台逻辑解耦后，你可以把 GPT-4o、DeepSeek 或一个跑在 Ollama 上的 7B 模型接到同一个 Agentic Center，无需修改代码（资料：[S6]）。

下一节，我们从工具集中选取三个关键工具——`DeviceTool`、`DriverTool`、`PointValueTool`——拆解具体实现，并演示一次完整的“对话驱动设备操作”。