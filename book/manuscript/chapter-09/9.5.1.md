### 9.5.1 MCP协议诞生背景与核心设计

MQTT、CoAP、LwM2M 和 OPC UA 的通信模型本质上是静态的：规则由平台定义，数据按 Topic 或资源路径流动，状态变更由设备或平台端触发。AI 应用在操作这些设备时，面临的不再是数据不可达问题，而是三个更深层的鸿沟。

**语义鸿沟**。MQTT 发布一条 `topic/dev/001/temp`，载荷是 `26.8`。AI 能收到这个值，但不清楚它是摄氏度还是华氏度、是瞬时值还是五分钟平均值、是正常范围还是异常告警。CoAP 的路径结构更规范一些，但字段含义仍依赖平台侧的物模型映射。AI 系统需要的不仅是数据流，还需要设备功能的元描述：哪些参数可读、哪些可写、写操作的约束条件是什么、返回值如何解读。

**安全边界与上下文缺失**。AI 应用直接通过 MQTT Client 订阅设备 Topic，要么获得过多权限（能读到其他租户的设备），要么缺乏控制操作的上下文（不知道目标设备是否处于可操作状态）。MQTT Broker 不会为一次 AI 对话维护会话状态、鉴权上下文和调用链。AI 处理一个问题通常需要多步推理，涉及多个设备或数据源，每个调用都必须携带已建立的安全上下文。

**状态管理不对称**。设备通信协议多为事件驱动或轮询模型：设备上报，平台消费。但 AI Agent 的推理是会话驱动的：它需要先理解当前状态，再决定下一步操作，操作后还需确认结果。MQTT 的发布—订阅模型对查询—响应模式天生不友好；CoAP 的请求—响应模型虽然接近，但没有标准方式描述“这个操作的返回值是另一个资源的引用，需要继续解析”。AI 需要一个有状态的、可发现能力边界的交互协议。

MCP（Model Context Protocol，模型上下文协议）正是在这一背景下产生的。它不是设备协议，而是一个专门面向 AI 应用与外部工具、资源和知识库交互的会话协议。以下描述基于 IoT DC3 项目文档中 MCP 实现的公开设计方向（资料：[S6][S12]），属于作者对该类协议在物联网中应用的工程判断，并非正式标准的最终定义，部分细节以示意性方式呈现。

**MCP的上下文模型与通信模型**

IoT DC3 的 MCP 实现将 AI 与外部系统的交互抽象为一个两端会话的上下文（资料：[S6]）。核心实体不是“消息”或“Topic”，而是资源（Resources）、工具（Tools）和提示（Prompts）：

- **Resources**：由服务端暴露的可读上下文，供模型在推理时引用。包括设备属性定义、物模型 JSON Schema、历史数据摘要或运维手册。它们通过 URI 标识，支持内容协商和增量读取。
- **Tools**：可由模型请求触发的可执行动作。每个 Tool 声明自己的输入 Schema，描述参数名称、类型、约束和是否必填。AI 模型不直接执行工具，而是生成调用请求，由 MCP 服务器校验权限、执行操作并返回结果。IoT DC3 的 MCP Server 通过聚合四个中心（设备、数据、告警、系统）的 OpenAPI 自动生成工具目录（资料：[S6]）。
- **Prompts**：可复用的、参数化的提示模板，允许服务端指导模型“如何理解这个领域的资源”。

IoT DC3 的 MCP 网关使用 JSON-RPC 2.0 交换消息（资料：[S6]）。客户端和服务端通过能力协商完成初始化，声明支持的协议版本和能力列表。服务端通过 `tools/list` 返回可用工具列表，客户端通过 `tools/call` 触发工具执行。整个交互是有状态的——每次调用携带一个 request ID，响应与请求在 session 内关联，支持超时和错误处理。通信走基于 Token 的安全认证（该项目采用 OAuth 2.1 机制，详见 [S6]），结合工具白名单与风险分级，确保每次调用都在既定安全策略下进行。

图9-5 展示了 MCP 协议在 IoT 场景中一次典型的交互序列，涵盖资源发现、工具调用和状态反馈。

```book-figure
id: "fig-09-05"
type: "sequence"
title: "图9-5 MCP协议交互序列图"
purpose: "展示AI Agent通过MCP与IoT平台及设备的典型交互流程：资源发现、工具调用、状态反馈。"
audience_takeaway: "读者应理解MCP作为AI与IoT平台之间的互操作层，不直接操作设备，而是通过平台封装的安全上下文调用后端服务。"
visual_focus: "从AI Agent发起会话到MCP Server鉴权、工具发现、调用后端设备服务、写入MQTT/CoAP，最终返回结果给AI Agent的完整时间线。"
design_level: "logical"
layout: "自上而下时间线，左侧参与者：AI Agent, MCP Server, IoT平台服务, 设备协议适配层, 物理设备。"
elements:
  - "AI Agent（发起侧）"
  - "MCP Server（会话管理、鉴权、工具路由）"
  - "IoT平台后端（设备管理、数据服务、告警服务）"
  - "设备协议适配层（MQTT Broker / CoAP端节点）"
  - "物理设备（传感器/执行器）"
relationships:
  - "AI Agent → MCP Server: session init (包括能力协商)"
  - "MCP Server → AI Agent: 返回session ID, 声明支持版本和能力"
  - "AI Agent → MCP Server: tools/list"
  - "MCP Server: 基于用户身份、租户范围聚合工具目录"
  - "MCP Server → AI Agent: 返回工具列表（每个含JSON Schema）"
  - "AI Agent → MCP Server: tools/call (选中的工具+参数)"
  - "MCP Server: 鉴权、白名单校验、风险分级"
  - "MCP Server → IoT平台后端: REST调用（设备服务/数据服务）"
  - "IoT平台后端 → 设备协议适配层: 按原链路下发（MQTT Publish / CoAP POST）"
  - "设备协议适配层 → 物理设备: 执行操作"
  - "物理设备 → 设备协议适配层: 响应/遥测上报"
  - "设备协议适配层 → IoT平台后端: 回调结果"
  - "IoT平台后端 → MCP Server: 返回结果（含状态、数据）"
  - "MCP Server → AI Agent: tools/call 响应（包含操作结果）"
regions:
  - id: "ai_domain"
    label: "AI应用域"
    role: "推理、会话管理、工具调用"
  - id: "platform_domain"
    label: "IoT平台域"
    role: "鉴权、设备管理、数据服务、安全策略"
  - id: "device_domain"
    label: "设备通信域"
    role: "设备协议适配、物理设备操作"
components:
  - id: "ai_agent"
    label: "AI Agent"
    type: "application"
    subtitle: "Claude Desktop等"
    group: "ai_domain"
    priority: "primary"
    shape: "actor"
  - id: "mcp_server"
    label: "MCP Server"
    type: "ai"
    subtitle: "会话管理、鉴权路由"
    group: "platform_domain"
    priority: "primary"
    shape: "card"
  - id: "iot_backend"
    label: "IoT平台后端"
    type: "platform"
    subtitle: "设备/数据服务"
    group: "platform_domain"
    priority: "primary"
    shape: "card"
  - id: "protocol_adapt"
    label: "协议适配层"
    type: "edge"
    subtitle: "MQTT/CoAP网关"
    group: "device_domain"
    priority: "normal"
    shape: "card"
  - id: "physical_device"
    label: "物理设备"
    type: "edge"
    subtitle: "传感器/执行器"
    group: "device_domain"
    priority: "normal"
    shape: "card"
connections:
  - from: "ai_agent"
    to: "mcp_server"
    label: "session init"
    style: "solid"
    direction: "request"
  - from: "mcp_server"
    to: "ai_agent"
    label: "session info"
    style: "solid"
    direction: "response"
  - from: "ai_agent"
    to: "mcp_server"
    label: "tools/list"
    style: "solid"
    direction: "request"
  - from: "mcp_server"
    to: "ai_agent"
    label: "tools list"
    style: "solid"
    direction: "response"
  - from: "ai_agent"
    to: "mcp_server"
    label: "tools/call"
    style: "solid"
    direction: "request"
  - from: "mcp_server"
    to: "iot_backend"
    label: "REST调用"
    style: "solid"
    direction: "request"
  - from: "iot_backend"
    to: "protocol_adapt"
    label: "MQTT/CoAP"
    style: "solid"
    direction: "request"
  - from: "protocol_adapt"
    to: "physical_device"
    label: "操作指令"
    style: "solid"
    direction: "request"
  - from: "physical_device"
    to: "protocol_adapt"
    label: "响应/遥测"
    style: "solid"
    direction: "response"
  - from: "protocol_adapt"
    to: "iot_backend"
    label: "结果回调"
    style: "solid"
    direction: "response"
  - from: "iot_backend"
    to: "mcp_server"
    label: "操作结果"
    style: "solid"
    direction: "response"
  - from: "mcp_server"
    to: "ai_agent"
    label: "tools/result"
    style: "solid"
    direction: "response"
callouts:
  - "MCP Server 不直接操作设备协议，而是通过 IoT 平台后端的安全链路。"
  - "tools/list 返回的工具列表是经过鉴权和租户裁剪的。"
  - "物理设备侧协议不变（MQTT/CoAP），MCP 增加的是 AI 与平台之间的互操作层。"
legend:
  - "实线箭头 = 同步调用（请求/响应）。"
  - "虚线箭头在本文中未出现，保持图例统一。"
caption: "图9-5 MCP协议交互序列示意：AI Agent通过MCP Server与IoT平台及设备进行有状态、安全可控的工具调用。"
visual_constraints:
  - "参与者不超过6个，每条消息带简短标签。"
  - "突出显示 MCP Server 的鉴权和聚合，用橙色强调。"
  - "图例放在底部，不遮挡时序线。"
render_notes: "HTML/SVG 渲染，浅色背景，时间线自上而下，参与者列在左侧，消息线用直角折线段，箭头上标注标签。"
```

这一设计与 MQTT 的无状态发布有本质区别：MCP 在会话上下文中运行，服务端可以基于用户身份、租户范围动态决定暴露哪些资源、开放哪些工具。AI 应用看到的不是一个固定的 Topic 树，而是一个经过鉴权和策略裁剪的工具目录。

**MCP与IoT平台的分工边界**

在 IoT DC3 项目中，MCP Server 部署在平台的适配层，而不是替换设备侧协议（资料：[S6]）。一个典型的调用链是：

- AI Agent（如 Claude Desktop 或自定义 Agent）启动一个 MCP 会话，通过 `tools/list` 获取当前用户可见的工具目录（资料：[S12]）。
- MCP Server 根据用户身份、租户范围和安全策略，从设备管理服务、物模型注册中心、数据历史服务等后端接口自动聚合出工具列表。每个工具的背后不是直接的 MQTT publish，而是经过基于 Token 的鉴权、工具白名单校验和风险分级的 REST 调用（资料：[S6]）。
- 当 Agent 调用一个“读取设备位号”工具时，MCP Server 不会直接向设备发送 CoAP 请求，而是经过 IoT 平台的权限服务、设备管理服务和数据服务，最终按平台已有链路下发命令或读取缓存值。

这一设计确保 MCP 不绕过已有的 IoT 安全治理。设备侧协议依然是 MQTT、CoAP、OPC UA 或 Modbus。MCP 增加的是 AI 与平台之间的互操作层，而不是对设备通信协议的重新发明。

**工程实践中应避免的误区**

实践中，团队容易把 MCP 当作“让 AI 直连设备”的捷径。最典型的设计失误是：MCP Server 直接维护 MQTT 连接池，Agent 调用工具时直接 publish 到设备 Topic。这种架构绕过了平台层的策略引擎、服务降级、租户隔离和联动逻辑，将双因子确认、写入频率限制和操作审计的义务交给了 AI Prompt。AI 模型不是确定的实时控制系统，任何绕过平台治理的调用链都应视作安全违规。

更稳妥的判断是：MCP 在物联网中的正确位置是 AI 应用与 IoT 平台之间的互操作层。它解决的是“AI 如何以统一协议发现和调用平台能力”，而不是“AI 如何替代 MQTT/CoAP 接管设备通信”。平台依然用 MQTT 收遥测，用 CoAP 管设备，用 OPC UA 做工业语义；MCP 只增加一层面向 AI 的工具抽象，让模型能在安全上下文中操作经过策略裁剪的平台能力。这两个栈之间不应直接短接，除非架构师愿意承担开环控制风险。

**延伸阅读**：第7章7.4节详细介绍了 IoT DC3 中 Agentic Center 的 MCP 实践，包括工具注册、会话管理和安全策略的配置方法。第8章8.3节讨论了 AI Agent 操作设备时的安全边界与审计方案。