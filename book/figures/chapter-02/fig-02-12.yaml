---
id: fig-02-12
type: architecture
title: 图2-12 DC3默认主链与受控智能扩展
purpose: 区分 IoT DC3 当前遥测与命令主链、外部 HTTP 访问链和可选 Agentic 扩展，纠正把设备流量经 Gateway 或把模型置于实时链路中的误解。
audience_takeaway: 遥测经 Driver、RabbitMQ、Data 持久化，外部请求经 Gateway 与 Auth；Agentic 只通过受权工具查询，写操作经策略和确认后进入 Data 命令链。
visual_focus: 上半区显示当前数据主链，下半区显示外部访问与受控智能扩展，确认边界使用虚线连接 Data。
design_level: implementation
layout: 上下双泳道；上层从现场设备水平流向 PostgreSQL，下层从操作员流向 Gateway、Auth、Agentic 和 Workflow，再经确认连接 Data。
elements:
  - 现场设备、协议 Driver、RabbitMQ、Data 与 PostgreSQL
  - 操作员、Gateway、Auth、Agentic 与策略 Workflow
  - PLC/SIS 硬实时与安全联锁边界
relationships:
  - 现场设备 → Driver → RabbitMQ → Data → PostgreSQL：当前遥测主链
  - Data → RabbitMQ → Driver → 现场设备：当前命令与回执主链
  - 操作员 → Gateway → Auth：平台登录与外部 API 访问
  - Agentic → Data：只通过受权 Tool 查询证据
  - Agentic → Workflow → Data：候选 Action 经策略与确认后提交命令
caption:
  zh: 图2-12 遥测与外部访问分走两条链，智能建议只有经过策略和确认后才能进入设备命令链。
  en: Figure 2-12 Telemetry and external access follow separate paths; an AI proposal enters the device-command path only after policy checks and confirmation.
render_notes: 自包含 HTML 与内联 SVG；上下双泳道，主链实线，受控扩展虚线；使用 style.yaml 调色板。
labels:
  en:
    图2-12 DC3默认主链与受控智能扩展: Figure 2-12 Default DC3 paths and governed AI extension
    默认遥测链路经协议 Driver、RabbitMQ 和 Data 持久化到 TimescaleDB；外部请求经 Gateway 和 Auth，Agentic 只通过授权工具查询，写操作经人工确认后进入 Data 命令链。: The default telemetry path persists through the protocol Driver, RabbitMQ, Data, and TimescaleDB; external requests go through Gateway and Auth. Agentic queries only through authorized tools, and writes enter the Data command path after human confirmation.
    遥测不经 Gateway，Agentic 不在实时数据主链: Telemetry bypasses Gateway; Agentic is not on the real-time data path
    当前遥测与命令主链: Current telemetry and command path
    现场设备: Field devices
    传感器与执行器: Sensors and actuators
    协议 Driver: Protocol Driver
    解析与位号映射: Parsing and point mapping
    RabbitMQ: RabbitMQ
    位号值、命令、回执: Point values, commands, receipts
    Data 中心: Data Center
    持久化与查询: Persistence and queries
    TimescaleDB: TimescaleDB
    默认时序适配器: Default time-series adapter
    ① 现场协议: ① Field protocol
    ② 位号值: ② Point values
    ③ 消费: ③ Consume
    ④ 持久化: ④ Persist
    ⑩ 确认后的命令经 RabbitMQ 返回 Driver: ⑩ Confirmed commands return to the Driver through RabbitMQ
    外部访问与可选智能扩展: External access and optional AI extension
    操作员 / 应用: Operator / application
    请求与最终确认: Requests and final confirmation
    Gateway: Gateway
    外部 HTTP 入口: External HTTP entry
    Auth: Auth
    平台主体认证: Platform-principal authentication
    Agentic: Agentic
    模型、会话与 Tools: Models, sessions, and Tools
    策略 / Workflow: Policy / Workflow
    校验、审批与审计: Validation, approval, and audit
    ⑤ 登录 / API: ⑤ Login / API
    ⑥ 认证: ⑥ Authenticate
    ⑦ 受权 Tool: ⑦ Authorized Tool
    ⑧ 候选 Action: ⑧ Proposed Action
    ⑨ 确认后调用 Data 命令接口: ⑨ Call the Data command API after confirmation
    只读查询: Read-only query
    "边界：PLC / SIS 承担硬实时与安全联锁；模型不能绕过策略、确认和平台命令链。": "Boundary: PLC / SIS owns hard real-time control and safety interlocks; models cannot bypass policy, confirmation, or the platform command path."
    图2-12 遥测与外部访问分走两条链，智能建议只有经过策略和确认后才能进入设备命令链。: Figure 2-12 Telemetry and external access follow separate paths; an AI proposal enters the device-command path only after policy checks and confirmation.
