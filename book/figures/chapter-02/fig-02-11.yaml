---
id: fig-02-11
type: dataflow
title: 图2-11 Data中心数据流示意
purpose: 展示 IoT DC3 从现场设备、Driver、消息端口、Data 到时序存储端口的遥测主链，并以 RabbitMQ 与 PostgreSQL/TimescaleDB 表示默认适配器。
audience_takeaway: Driver 把位号值发布到消息端口，Data 消费并经时序存储端口持久化；Gateway 不在遥测主链，Agentic 等消费者也不直接连接内部消息队列或数据库。
visual_focus: 从现场设备到 PostgreSQL 的单向主链，以及 PostgreSQL 到可选冷存储、Data 到授权消费者的扩展出口。
design_level: implementation
layout: 水平左到右主链，PostgreSQL 下方放可选归档，最右侧为受控消费者。
elements:
  - 现场设备与协议 Driver
  - RabbitMQ、Data 中心与 PostgreSQL
  - 可选冷存储与授权消费者
relationships:
  - 现场设备 → Driver：现场协议数据
  - Driver → RabbitMQ：发布位号值
  - RabbitMQ → Data：消费消息
  - Data → PostgreSQL：持久化
  - Data / PostgreSQL → 授权消费者：经 API、WebSocket 或 Tool 访问
caption:
  zh: 图2-11 默认链路使用 RabbitMQ 与 TimescaleDB；消费者只通过受控出口访问。
  en: Figure 2-11 The default path uses RabbitMQ and TimescaleDB, while consumers access data only through governed interfaces.
render_notes: 保持既有水平 SVG 布局；使用 style.yaml 调色板；实线表示当前主链，虚线表示可选归档。
labels:
  en:
    图2-11 Data中心数据流示意: Figure 2-11 Data Center data flow
    现场设备数据经 Driver 归一后进入消息端口，Data 消费并经时序存储端口持久化；图中的 RabbitMQ 与 PostgreSQL 表示默认适配器。: Field-device data enters the message port after Driver normalization and is persisted by Data through the time-series storage port; RabbitMQ and PostgreSQL depict the default adapters.
    Driver 发布，Data 消费；Gateway 不在遥测主链: Drivers publish and Data consumes; Gateway is not on the telemetry path
    设备与边缘域: Device and edge domain
    消息与平台服务域: Messaging and platform-services domain
    数据资产域: Data asset domain
    原始信号: Raw signal
    发布位号值: Publish point values
    消费消息: Consume messages
    持久化: Persist
    归档扩展: Archive extension
    受权查询 / 订阅: Authorized query / subscription
    现场设备: Field devices
    PLC · 电表 · 传感器: PLCs · meters · sensors
    驱动模块: Driver module
    南向协议驱动: Southbound protocol Driver
    RabbitMQ: RabbitMQ
    位号值 Exchange: Point-value Exchange
    Data 中心: Data Center
    消费、持久化与查询: Consume, persist, and query
    PostgreSQL: PostgreSQL
    当前默认存储: Current default store
    对象 / 冷存储: Object / cold storage
    可选归档扩展: Optional archive extension
    授权消费者: Authorized consumers
    API / WebSocket / Tool: API / WebSocket / Tool
    设备与边缘: Devices and edge
    平台服务: Platform services
    消息队列（缓冲）: Message queue (buffer)
    存储 / 订阅方: Storage / subscribers
    同步 / 即时调用: Synchronous / immediate call
    异步 / 事件驱动: Asynchronous / event-driven
    图2-11 默认链路使用 RabbitMQ 与 TimescaleDB；消费者只通过受控出口访问。: Figure 2-11 The default path uses RabbitMQ and TimescaleDB, while consumers access data only through governed interfaces.
