---
id: fig-02-10
type: flowchart
title: 图2-10 可选ECA Workflow：告警与受控动作
purpose: 展示独立规则或 Workflow 如何消费位号事件，并区分告警通知与需要策略、确认和审计的设备写操作。
audience_takeaway: ECA 是项目可选扩展，不是 Manager 的默认事实；规则服务不直连硬件，候选 Action 必须经治理后进入 Data 命令链。
visual_focus: 规则节点向告警和候选 Action 分叉，候选 Action 经策略确认后回到命令总线。
design_level: logical
layout: 水平主链从设备到规则服务，上方为告警分支，下方为确认后回到命令总线的回路。
elements:
  - 温度传感器、协议 Driver 与 RabbitMQ
  - 可选规则或 Workflow
  - 告警通知与策略确认
relationships:
  - 温度传感器 → Driver → RabbitMQ → 规则服务：位号事件输入
  - 规则服务 → 告警通知：无设备副作用的告警链
  - 规则服务 → 策略与确认 → RabbitMQ：受控命令链
caption:
  zh: 图2-10 可选规则服务消费位号事件；告警走通知链，设备动作经策略与确认后回到命令总线。
  en: Figure 2-10 An optional rule service consumes point events; alerts use the notification path, while device actions return to the command bus only after policy checks and confirmation.
render_notes: 保持既有水平 SVG 布局；告警使用危险色虚线，数据和受控命令使用主色实线。
labels:
  en:
    图2-10 可选ECA Workflow：告警与受控动作: Figure 2-10 Optional ECA Workflow for alerts and governed actions
    温度事件经 Driver 和 RabbitMQ 进入可选规则服务，告警可直接通知，设备写动作必须经过策略、确认和审计后回到命令总线。: Temperature events enter an optional rule service through the Driver and RabbitMQ. Alerts can notify directly, while device writes must return to the command bus after policy checks, confirmation, and audit.
    参考扩展，不代表 Manager 内置规则引擎: Reference extension, not a built-in Manager rule engine
    现场数据: Field data
    发布事件: Publish event
    订阅输入: Subscribe input
    告警事件: Alert event
    候选 Action: Proposed Action
    确认后进入命令总线: Enter command bus after confirmation
    温度传感器: Temperature sensor
    现场设备: Field device
    协议 Driver: Protocol Driver
    解析与位号映射: Parsing and point mapping
    RabbitMQ: RabbitMQ
    事件与命令总线: Event and command bus
    规则 / Workflow: Rule / Workflow
    可选 ECA 扩展: Optional ECA extension
    策略与确认: Policy and confirmation
    值域 · 互锁 · 审批: Range · interlock · approval
    告警通知: Alert notification
    通知渠道与告警状态: Notification channels and alert state
    规则服务不直连硬件：写操作经策略确认后，由 Data 命令接口与 Driver 链路执行。: The rule service does not connect directly to hardware. Writes run through the Data command API and Driver path after policy confirmation.
    平台服务: Platform service
    决策节点: Decision node
    执行动作: Action
    告警动作: Alert action
    数据/命令流: Data / command flow
    告警流: Alert flow
    图2-10 可选规则服务消费位号事件；告警走通知链，设备动作经策略与确认后回到命令总线。: Figure 2-10 An optional rule service consumes point events; alerts use the notification path, while device actions return to the command bus only after policy checks and confirmation.
